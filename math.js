//math is a bulit in object that provides a collection and methods

let x = -3.5 ;
let y = 2 ;
let a = 45 ;
let z ; 

//z = Math.round(x); // roundup 
//z = Math.floor(x); //rounddown
//z=Math.ceil(x); //opposite of floor
//z = Math.trunc(x);   //removes the  decimal point 
//z= Math.pow(x, y) ;
//z=Math.sqrt(x);
//z= Math.sin(a)
//z=Math.abs(x);
//z=Math.sign(x);

let max = Math.max(x,y,a);
let min = Math.min(x,y,a);
console.log(max);
console.log(min);
